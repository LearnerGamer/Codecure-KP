## Packages
recharts | Dashboard analytics charts for Admin view
framer-motion | Smooth page transitions and entry animations
date-fns | Formatting timestamps for patient records

## Notes
The AI Doctor prediction logic is implemented client-side for immediate feedback as requested, but also aligns with the /api/ai/predict endpoint structure.
Tailwind config should use the CSS variables defined in index.css for consistent medical theming.
